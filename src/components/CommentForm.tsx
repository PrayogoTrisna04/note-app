import { useState } from "react";
export default function CommentForm({noteId,onComment}:{noteId:string,onComment:(c:any)=>void}){const [content,setContent]=useState("");const submit=async()=>{if(!content.trim())return;const res=await fetch("/api/comments",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({noteId,content})});if(res.ok){const data=await res.json();onComment(data);setContent("");}};return(<div className="mt-4"><textarea value={content} onChange={(e)=>setContent(e.target.value)} placeholder="Add a comment..." className="w-full border p-2 rounded"/><button onClick={submit} className="bg-blue-600 text-white px-3 py-1 rounded mt-2">Submit</button></div>);}